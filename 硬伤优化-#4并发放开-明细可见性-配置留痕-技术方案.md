# 硬伤优化·第二批技术方案：#4 放开并发 + P2-6 明细可见性 + P2-7 配置留痕 + P2-8 意图确认

> 来源：`评测平台分析-硬伤与优化清单.md`（2026-08-31）剩余项；第一批（#1 多数决 / #3 子集重跑 / #6 N/A 保护 / #2 档位化）已落地。
> 用户圈定范围：优化#4 + P2-6+5+7 + P2-7+8 + P2-8 意图确认，四块全做。
> **本版已过独立 Plan subagent 只读评审（P1-1/P1-2 阻断点 + P2 七项已修订吸收）。**
> 三块（A/B/C）相互独立可分批交付；D 是意图确认决策，其结果决定是否扩大改动面。
> **决策点已定夺（2026-08-31）：D1 采用、D2 接受 per-run 桶、D3 保守分级、D4 移除、D5 500。** 方案定稿，可拆任务实施。

---

## 模块 A：优化#4 放开并发（同 agent 多 run + 判分不占执行槽）

### 问题（硬伤 P1-5 后半）

- `runs.py:42` `_ACTIVE_STATUS = ("pending", "running", "scoring")`，同时服务三处：
  1. `runs.py:145` create_run 互斥——同 agent 任一 active → 409
  2. `runs.py:228` rerun_run 互斥——同上
  3. `runs.py:201` cancel 可取消判断——`status not in _ACTIVE_STATUS → 400 不可取消`
- **scoring 也占执行槽**：judge 队列可能数小时（判分期间）同 agent 无法开新 run（硬伤清单 P1-5）。
- 无并发上限配置：同 agent 硬编码 1 个活跃 run。

### 设计

**1. 状态槽拆分（互斥槽 ≠ 可取消槽）**

- 新增 `_MUTEX_STATUS = ("pending", "running")`：仅「执行中」占用互斥槽（创建/重跑判断用）。
- `_ACTIVE_STATUS` **保留原值**（含 scoring）：cancel 判断不变——scoring 期的 run 仍可取消。
- 语义核对（cancel 与 score_run 竞态）：score_run 持 run 行 FOR UPDATE（scorer.py:226）；cancel 的 `db.get` 普通读 + `status not in _ACTIVE_STATUS` 检查。score_run 先提交 → cancel 读到 completed → 400；cancel 先提交 → score_run 读到 cancelled → 幂等返回。均无破坏。✓
- ⚠️ **注意「N=1 行为不变」的准确表述**：默认 N=1 时，唯一的行为变化就是 **scoring 不再占互斥槽**（这是模块目的，本就是行为变更）。回归测试必须覆盖「N=1 + 已有 scoring run → 可开新 run」。除此之外与现状一致。

**2. 新增配置 `max_active_runs_per_agent`（global scope，default 1）**

- seed.py 注册，meta `{"type":"int","min":1,"max":8}`（⚠️ 收窄上限：per-run 桶下 N 是并发放大镜，N=8×per_agent_concurrency=3 → 单 agent 24 并发 execute 已偏高，N=100 一次 PUT 可自伤）。
- 互斥计数：`SELECT id ... WHERE agent_id AND status.in_(_MUTEX_STATUS) FOR UPDATE` 由 `.first()` 改 `.all()` 计数，`count >= N → 409`。
- ⚠️ **A1 实现陷阱（评审 P2-1）**：create_run 热读 `SystemConfig` scope=global 该 key 时，未 seed 库（sqlite 集成测试不 seed SystemConfig）该行不存在 → 必须 `cfg.value if cfg else 1` 兜底默认，否则 `None.value` 崩。测试补「配置缺行 → 默认 1」。
- global scope 不进 `_snapshot_run_config`（runs.py:62-65 只取 scope=run），互斥判断直接热读，不重启生效。
- **rerun 同源**：runs.py:228 同样改计数。
- ⚠️ **A4 rerun 护栏（评审 P2-4）**：N>1 时计数放行可能对**仍 running/pending 的源 run** 并行重跑（前端已用 `!isActive` 门住，但 API 直连可绕过）。rerun 增加显式拒绝：源 run.status ∈ `_MUTEX_STATUS` → 400「源 run 执行中，不可重跑」。

**3. 跨 run 并发语义（写明的限制）**

- KeyedLimiter 是 **per-run 桶**（limiter.py:35-67）：N>1 时同 agent 跨 run 并发上限 = N×per_agent_concurrency。
- 评审确认的安全网：① httpx 客户端每 run 一个、连接池独立（http.py:137-143）；② reset(seed) 段 agent_mutex 跨 run 串行（orchestrator.py:333）；③ 单进程 asyncio 全量 gather 是纯异步网络 IO，不耗线程；④ agent 容器单会话串行场景 N>1 反而安全（agent 侧排队）。
- ⚠️ **D2 决策**：接受此限制 + 默认 N=1（推荐）；或加 agent 级全局桶（新机制，本批不做）。
- ⚠️ **global_max_inflight 已被架空（评审 P2-6，风险段声明）**：per-run 桶让 `global_max_inflight` 从「平台总量闸门」退化为「单 run 上限」（7.8 前置④ 引入的既有偏差，模块 A 放大）。平台级硬顶需新共享信号量，**非本批**——风险段显式声明，防后人误以为 global_max_inflight=16 仍是全站天花板。
- `estimate_run_timeout` 的 active_runs 分母（orchestrator.py:157-169）保留，但评审指出两个不精确，风险段声明：
  - 分母是**全局**在途数而非**同 agent** 在途数（对「同 agent 并发」这个目标场景不对症）；
  - 只在 run 启动瞬间采样——run1 先启动拿全量预算，run2 后加入争抢 → run1 实际耗时变长，极端场景（高并发 + 用例普遍吃满超时）有被 scanner ②（scanner.py:50-66）误标 timeout 的残余风险（余量 1.5×(retries+1) 通常覆盖）。
- ⚠️ **A6 DB 连接池头寸（评审 P2-6）**：db.py:12-19 `pool_size=10/max_overflow=10` 按 max_inflight(16) 配。放开并发后多 agent N>1 时每 case 多次开 SessionLocal，池压力上升——**本批不动池，风险段声明观测**。

### 测试

- 单测（新增 `tests/test_run_mutex_n.py`）：
  - N=2 时 1 个活跃可创建、2 个活跃 → 409；N=1 回归（pending 或 running 存在 → 409）
  - **scoring 不占互斥槽**（N=1）：已有 scoring run → 可创建；已有 running → 409
  - **配置缺行 → 默认 1**（未 seed 库兜底）
  - rerun：计数 + 源 run 执行中 → 400
- 集成适配：`tests/integration/test_integration_mutex.py:125,197` 断言 pending ∈ `_ACTIVE_STATUS`——`_ACTIVE_STATUS` 保留原值，**断言不动**；新增 scoring 不占槽集成用例（可选）。

### 改动文件

| 文件 | 改动 |
|---|---|
| `backend/app/api/runs.py` | 拆 `_MUTEX_STATUS`；create_run/rerun 互斥改计数 + 热读 N（兜底默认）+ rerun 源执行中护栏 |
| `backend/app/seed.py` | 注册 `max_active_runs_per_agent`（global, default 1, max 8） |
| `frontend/src/constants/configMeta.js` | 补该 key 条目（label/desc，⚠️ 评审 P2-2，否则配置中心兜底显示原始 key） |
| `backend/tests/test_run_mutex_n.py` | 新增 |
| `backend/tests/integration/test_integration_mutex.py` | 新增 scoring 不占槽用例 |

---

## 模块 B：P2-6 明细可见性 + 优化#5/#7（run 主视图内联 + 证据分级 + top 扣分原因）

### 问题

- `run_results`（runs.py:251-284）只返回 `pass_fail/score_total/score_per_dimension/error_*`，**不含 answer/assertion_results/judge_results** → 主视图看不到 agent 原始输出与断言明细（P2-6）。
- 证据接口 `result_evidence`（runs.py:340-365）有完整字段，但 `deps.py:93-95` `viewer_sees_evidence` = 仅非 viewer 可达。
- ⚠️ **B4 事实修正（评审）**：`/failures` 的 `assertion_failures` **现已无条件返回**（runs.py:333，含 actual/args），仅 `judge_failures` 按 viewer 裁剪（runs.py:334）。即 viewer 今天就能通过 /failures 看到断言失败明细。D3 的「泄露面」讨论必须以此为基线，不是净新增。
- `/failures`（runs.py:287-337）无「top 扣分原因」聚合（优化#7）。

### 设计

**1. run_results 主视图内联（优化#5）**

- 每行追加字段（按证据权限裁剪）：
  - `answer`：截断 500 字符（⚠️ 复用 `assertions._truncate`（assertions/run.py:43-47），与断言 actual 同口径，D5）
  - `assertion_results`：失败断言（`pass==False`，含 actual）——聚焦「挂了什么」，成功断言折叠
  - `judge_results`：`[{dimension, score, reason}]`（reason 证据级）
- ⚠️ **B2 查询改造（评审 P2-2）**：现 `select(EvalResult)`（runs.py:262-263）会把完整 answer（未截断，可能数 KB/条）整列拉进内存再截。大 run（数百 case）字节量显著上升。改为**列子集查询**：`select(EvalResult.id, case_id, pass_fail, score_total, score_per_dimension, error_type, error_detail, func.left(EvalResult.answer, 500).label("answer_preview"), assertion_results, judge_results)`——DB 层截断，避免整列 blob 出库。

**2. 证据权限分级（⚠️ 待确认 D3）**

- 现状：`viewer_sees_evidence = role != viewer`（deps.py:93-95）——viewer 全禁，staff 全见。
- 拆两级：
  - `sees_evidence_basic`（所有登录用户含 viewer）：**断言明细（现状已暴露）+ answer（截断500）**——「为什么扣分」的定位信息。
  - `sees_evidence_full`（staff，现状不变）：reasoning 全文、tool_calls、usage、**judge reason（⚠️ 评审建议：judge reason 划入 basic 需产品拍板，默认归 full 保守）**。
- 落地：`result_evidence` 与 `run_results` 内联按角色裁剪；`run_failures` 的 `judge_failures`/`top_reasons` 跟随 basic/full。
- ⚠️ 泄露面（修正 B4 基线）：basic 净新增 = answer（截断 500）+ 可能的 judge reason。answer 可能含业务敏感数据（agent 评测回答常复述内部资料）——这是唯一实质新增泄露面。

**3. /failures top 扣分原因聚合（优化#7，⚠️ 按评审 B1 修订）**

- ⚠️ **不能按 judge reason 自由文本精确分组**：reason 是 LLM 自由文本（rubric.py:48-51），同因措辞几乎不重复 → 每组 count≈1 碎片化。改为**按 dimension_code 为主聚合键**：
  - `top_reasons: [{dimension, count(命中用例数), sample_reasons(去重前 3 条 reason), avg_score}]`
  - 聚合对象：fail 用例的 `judge_failures`，按 dimension 分组；按 count 降序取 top N。
- 权限：跟随 judge_failures 可见性。
- 前端 drawer L3.5 顶部加「top 扣分原因」区块（维度 + count + 代表 reason）。

**4. 前端 Dashboard.vue**

- drawer 用例明细表格加「Answer 摘要」列（截断预览，可见性随 D3）+ 失败用例行内展开断言明细。
- L3.5 顶部 top_reasons 区块。
- ⚠️ **B3（评审 P2-3）**：evidence dialog 的 Usage/Timing 区块加 `v-if="evidence.usage || evidence.timing"`（Dashboard.vue:555-557 附近），否则 viewer 打开时被裁剪字段 `JSON.stringify` 渲染 "{}"。

### 测试

- run_results 内联字段断言（answer 截断、失败断言过滤、judge_results 结构、列子集查询不含整列 answer）
- 权限裁剪：viewer 看不到 reasoning/tool_calls/usage（basic 分支），staff 可见
- top_reasons：按 dimension 聚合、count/sample_reasons/avg_score 正确、权限裁剪

### 改动文件

| 文件 | 改动 |
|---|---|
| `backend/app/api/runs.py` | run_results 列子集 + 内联 + run_failures top_reasons |
| `backend/app/api/deps.py` | 证据权限拆 basic/full 两级 |
| `frontend/src/views/Dashboard.vue` | Answer 列 + 断言内联 + top 扣分区块 + Usage/Timing v-if |
| `backend/tests/` | 内联 + 聚合 + 权限用例 |

---

## 模块 C：P2-7 配置留痕 + 优化#8（targets/weights 审计 + 变更历史）

### 问题

- `set_targets`（agents.py:407-438）有双签状态机（approved_by_1/2、approval_status），**无变更历史、不写审计**。
- `set_agent_weights`（agents.py:352-366）、`set_interface_weights`（agents.py:377-390）同样无审计。
- target/权重从多少改成多少、谁改的，无痕可查（P2-7）。

### 设计

**1. 三个接口写审计（同事务，write_audit 不 commit，随业务 commit）**

- action 命名：`agent.weights.set` / `agent.interface.weights.set` / `agent.targets.set`。
- 统一：`target_type="agent"`，`target_id=str(agent_id)`。
- ⚠️ **P1-2 修订（评审阻断点）**：detail 必须带 `interface_id`，否则 config-history 无法区分「agent 默认权重（interface_id=0）」与「某接口覆盖值」的同名维度变更。detail 结构：
  ```
  {"interface_id": <iid>, "dims": {<dimension_code>: {"from": 旧值, "to": 新值}}}
  ```
- ⚠️ **C1（评审 P2-1）**：from 值须在 `row.weight = w` / `row.target_score = score` **之前**缓存旧值（现为逐 dim select-then-update）。
- targets 额外记 `approval_status: 旧→新` 与操作人（双签两次修改各落一条）。
- `set_targets` 需加 `request: Request` 依赖（现签名无 request，write_audit 需要）。

**2. 变更历史查询接口 `GET /agents/{id}/config-history`（staff 可读）**

- 查 `audit_log WHERE target_type="agent" AND target_id=str(id) AND action IN (三 action) ORDER BY created_at DESC`。
- 返回：`[{id, action, interface_id, dims, ip, created_at, username}]`（join User 取名）。
- ⚠️ **C2（评审 P2-2）**：AuditLog.user_id 是裸 Integer 无 FK（misc.py:51），用户删除后 username 为 None，前端要容忍 + 后端兜底 `or "已删除用户"`。
- 无需新表——AuditLog.detail JSON 已承载 before/after 快照。
- ⚠️ **审计保留窗口（评审问题，风险段声明）**：cleanup_rules 是否清 audit_log 需确认；若清理，config-history 有保留窗口。

**3. 前端 Agents.vue**

- 权重编辑区 / 达标分编辑区加「变更历史」入口 → 抽屉展示该 agent 的 config-history（按接口分组展示）。

### 测试

- 三个接口写审计断言（detail 含 interface_id + from/to、target_id 正确、随事务提交）
- set_targets 双签两次修改各落一条（approval_status 变化记录）
- config-history：按 agent/action 过滤、倒序、join username 兜底

### 改动文件

| 文件 | 改动 |
|---|---|
| `backend/app/api/agents.py` | 三接口写审计（detail 带 interface_id）+ config-history 接口（加 request 依赖） |
| `frontend/src/views/Agents.vue` | 变更历史抽屉 |
| `backend/tests/` | 审计 + 历史用例 |

---

## 模块 D：P2-8 assertion_penalty 死配置意图确认（⚠️ 待确认 D4）

### 现状（评审确认）

- seed.py:369 注册 `assertion_penalty=30`（scope=run）；前端 configMeta.js:18 有 label「断言失败扣分」。
- 全仓库代码（backend）**仅 seed 引用，无消费方**（grep 确认）。
- 产品意图在文档有记录：solution_detail.md:832「断言作为扣分项（不否决）：每挂一条扣 assertion_penalty 分；维度分 = max(0, judge等级×100 − 挂断言数×assertion_penalty)」；task.md:282 同。**意图已定稿但 scorer 未实现**。

### ⚠️ P1-1 阻断（评审）：D4-a「落地」按方案口径是 no-op，且与评分模型冲突

1. **永不触发**：全仓（seed_data.py:37、tests、skeleton.py:11、集成 helpers.py:94）所有结构断言 `dimension` 只取 **completeness/tool_usage**（规则维度），无任何断言 tagged 语义维度（factuality/reasoning_quality）。语义维度只出现在 judge_results。→「语义维度有失败断言则扣分」在现有数据下触发次数恒为 0。
2. **若改成「任意维度断言挂条扣分」**：与 `CompletenessMetric`（metrics/accuracy/completeness.py:18-26）通过率计分**双重扣分**（completeness 分本就是 passed/total×100）。
3. **门禁翻转**：即便补上断言→语义维度映射，扣分后 `_gate_met` 的 `int(value//20)` 档位化（scorer.py:38-51）会把 100-30=70（档 3）对 target 80（档 4）判 fail——与「不否决」意图矛盾，需额外的「扣分后门禁豁免」语义，方案未定义。

**结论**：D4-a 不可按现方案落地。选项：
- **D4-b 移除（推荐）**：删 seed 注册 + 删前端 configMeta 条目 + **清理存量 system_config 行**。
- **D4-c 重设计落地（独立批次）**：先补两张设计表（断言 dimension→语义维度映射规则、扣分后门禁豁免语义），改动面远超「score_case + 测试」。
- **D4-c 暂缓**：保留死配置——与项目自定原则（硬伤清单 #9「要么落地要么移出配置声明」）冲突，不推荐。

### ⚠️ D1（评审）：D4-b 移除 ≠ 只删 seed

seed 是 insert-if-missing（seed.py:415-422），存量库已有 `assertion_penalty` 行不会因删 seed 消失。若不清理 DB 行：`get_global_config`（config.py:29-32）仍列出该 key，而 `put_global_config` 因 meta 缺失报「拒绝热改」（config.py:45）→ **永久锁死的配置项**。D4-b 必须三步齐做：删 seed 注册 + 删前端 configMeta + 清理存量 system_config 行。

---

## 决策点汇总（已定夺 2026-08-31）

| # | 模块 | 决策 | 定夺 |
|---|------|------|------|
| D1 | A | `max_active_runs_per_agent` key/scope=global/default=1/热读 + 上限 max 8 | ✅ 采用；注意「N=1 行为不变」仅对执行互斥成立，scoring 剔除本身就是行为变更 |
| D2 | A | N>1 跨 run 并发超限 | ✅ 接受 per-run 桶（默认 N=1，global_max_inflight 已架空在风险段声明），平台级硬顶留后续 |
| D3 | B | viewer 可见范围 | ✅ 保守分级：basic = 断言（现状已暴露）+ answer（截断500）；reasoning/tool_calls/usage/**judge reason** 归 full（staff） |
| D4 | D | assertion_penalty | ✅ **移除（三步）**：删 seed 注册 + 删前端 configMeta + 清理存量 system_config 行 |
| D5 | B | answer 内联截断长度 | ✅ 500，复用 `assertions._truncate`（与断言 actual 同口径） |

---

## 风险

- **A**：N>1 跨 run 并发 = N×per_agent_concurrency（per-run 桶）；reset 段 agent_mutex 串行兜底；agent 单会话串行 N>1 反而安全。
- **A**：`global_max_inflight` 已是 per-run 桶下的「单 run 上限」而非平台总量闸门（7.8 前置④ 既有偏差，本批放大）——风险段显式声明。
- **A**：estimate_run_timeout 分母是全局在途数（非同 agent）+ 只在启动瞬间采样——极端高并发场景 run1 有被 scanner ② 误标 timeout 的残余风险（余量 1.5×(retries+1) 通常覆盖）。
- **A**：DB 连接池 `pool_size=10/max_overflow=10` 头寸薄，放开并发后观测（本批不动池）。
- **B**：answer 内联体积（DB 层截断 + 列子集查询控制）；权限放开的实质新增泄露面 = answer（截断 500）。
- **C**：audit_log 保留窗口取决于 cleanup_rules（若清理则历史有窗口，实施前确认）。
- **D**：D4-b 需三步齐做（seed + 前端 + 存量行清理），漏任一步 → 锁死配置项。

## 验证

1. 单测全绿（新增 test_run_mutex_n + 内联/聚合/审计用例 + 现有回归）
2. 前端 `cd frontend && npx vite build` 通过
3. 集成冒烟（发布验证）：同 agent 开 2 run（N=2）、scoring 期开新 run（N=1）、viewer 视角看 run_results/evidence（无 reasoning/tool_calls）、set_targets 后查 config-history（含 interface_id）
4. 存量库：max_active_runs_per_agent 走 seed insert-if-missing 不覆盖存量——default 1 时无需 SQL，调大需配置中心 PUT；D4-b 需手动清理 assertion_penalty 存量行

## 改动文件汇总

- backend：`api/runs.py`、`api/agents.py`、`api/deps.py`、`seed.py`
- frontend：`views/Dashboard.vue`、`views/Agents.vue`、`constants/configMeta.js`
- tests：新增 `test_run_mutex_n.py` + 内联/聚合/审计用例 + 集成适配
- （D4-b 追加）存量 `system_config` 清理 SQL：`DELETE FROM system_config WHERE \`key\`='assertion_penalty';`
