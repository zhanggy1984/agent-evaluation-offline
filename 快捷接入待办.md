# agent-evaluation-offline 快捷接入待办（URL 即接入）

> 目的：本文件是**「URL 即接入」目标的唯一真相源**。长会话 compact / 新开会话后，以本文件为准继续推进，逐项打勾。
> 更新：2026-08-30（目标 + 路线 + 剩余任务清单落地；阶段 1/2 已完成）
> 关联：[[上线待办.md]]（上线前整改）、`memory/aeo-quick-onboarding-route.md`

## 目标（用户原话，2026-08-30）

> 未来新 agent 很方便快捷地接入本项目评测，甚至配置了服务的 URL 以后自动接入。**接入标准由本项目指定，其他所有接入 agent 共同遵守和适配。**

## 路线决策（用户拍板）

1. **接入标准统一**：4 家现有 agent（cs/cc/gq/sp）**也迁移到 manifest v2**，不做双标准（推翻「仅新 agent 用 v2」的初案）。
2. **manifest（v2）成为接入真相源**：adapter_config 由 manifest 派生，消除 seed_data 手写 adapter_config 与 agent 侧 manifest 的并行双维护（待 Q3 落地方案确认静态化机制）。
3. manifest v2 已转正（`e77d017`：`app/core/contracts_v2.py` + 占位符引用校验，等价性/向后兼容三问全绿；`verify_manifest_v2.py` 验证脚本转正后已删）。

## 规则

- 每完成一项：状态改 `✅`，末尾追加提交号
- 每项先出方案 + 决策点，确认后再动手（A 级任务，涉及核心代码/跨服务）
- 从验证/走查发现的新问题，追加到本文件并附发现时间

## 总览

| 编号 | 项 | 状态 |
|---|---|---|
| Q0 | 目标 + 路线 + 任务清单落地（本文档） | ✅ 本文件 |
| Q1 | manifest v2 schema 定稿 + 解析器转正 + 占位符引用校验 | ✅ e77d017 |
| Q2 | scaffold discover 支持 v2：拉 manifest → 自动生成 adapter_config 草案 → 确认落库 | ✅ ae1cffc |
| Q3 | 4 家迁移 v2 + seed 适配（manifest 单一真相源，adapter_config 派生） | ⬜ 待方案 |
| Q4 | 文件型接口探测输入（cc 类：探测样例文件机制） | ⬜ 待方案 |
| Q5 | 用例骨架半自动（v2 `{{input.*}}` 域 → case 输入模板；scenes → 示例 suite 骨架） | ⬜ 待方案 |
| Q6 | 新 agent 接入文档（v2 manifest 写法 + SSE §5.1 达标指南） | ⬜ 待方案 |
| Q7 | 前端接入 wizard（填 URL → discover → 确认 → 凭证 → 冒烟 → adapter/用例骨架 → 落库） | ⬜ 待方案 |
| Q8 | 测试闭环（v2 解析单测、scaffold v2 集成、4 家迁移回归、wizard 前端测试） | ⬜ 贯穿各 Q |

## 已完成（阶段 1/2 基础）

- 阶段 1（L1 自动发现）：`GET /api/contracts`（4 家已实现）+ `POST /agents/{id}/discover`（parse/diff/sync/scenes）+ `app/core/contracts.py` 解析器。**已有**
- 阶段 2（L2 冒烟探测，2026-08-30 `6c15363`）：`POST /agents/{id}/probe` 探测输入三级来源（显式 body.input → `adapter_config.probe.input` → suite 首个 active case），新 agent 无 suite/case 也可冒烟；`resolve_probe_input` + 8 单测。单测 477 / 集成 74 全过。
  - 探测用最小 case：`TestCase(input=probe_input)` 不入库，仅作模板渲染源

---

## Q1 manifest v2 schema 定稿 + 解析器转正

- **现状**：`verify_manifest_v2.py`（已入库 72efd3a）内 V2Manifest/V2Prepare/V2Request/V2Contract + `build_adapter_config` 为提案实现，验证三问全绿（向后兼容 4/4、充分性 4/4、等价性 4/4 与 seed_data 逐字符相等）。尚未落 `app/core/contracts.py`。
- **方案要点**：把验证脚本中的模型与生成器转正为 `app/core/contracts_v2.py`（或并入 contracts.py）；`{{input.*}}/{{auth.*}}/{{prepare.*}}` 转换 `_conv` 转正；v1 解析不回归。
- **核心设计：占位符引用校验**（转正重心，防自动接入坏 adapter）：
  - `{{prepare.X.f}}` → X 必须是已声明的 prepare 步骤，且该步骤 `extract` 声明了字段 f，否则拒绝（含 poll 步骤产物引用、缺字段名 `{{prepare.X}}`）
  - `{{auth.*}}` → 与平台凭证配置对齐（注册 agent 时的 secrets）
  - `{{input.*}}` → 汇总为「接口需要的用例输入字段域」（`input_fields`，嵌套路径收全路径），供用例骨架（Q5）与用例校验使用
  - 校验覆盖 request + prepare + **sse** 全部渲染域（hook 实测抓到 sse 漏扫盲区，已补）
- **决策点**（已确认，按推荐项落地）：
  1. 校验失败「拒绝生成」（防坏 adapter 入库）；extract 未引用仅软警告 ✅
  2. 独立模块 `app/core/contracts_v2.py`（不并入 contracts.py）✅
  3. poll-only step（cc wait_done 无 path）schema 保持（V2Prepare.path 可空）✅
- **已落地**：`e77d017`——`app/core/contracts_v2.py`（模型 + `_conv`/`_render` + `_collect_refs`/`_validate_refs` + `build_adapter_config`→`AdapterDraft`）+ `tests/test_contracts_v2.py` 25 单测（schema/引用校验/等价性 4/4 逐字符等于 seed_data/元信息/v1 兼容）；全量单测 502 passed, 75 skipped；`verify_manifest_v2.py` 已删。
- **状态**：✅ 完成

## Q2 scaffold discover 支持 v2

- **现状**：discover 只解析 v1 manifest（interfaces/scenes），返回 diff；sync 手工补录接口；adapter_config 全程手工。
- **方案要点**：discover 识别 `contract_version: "2.0"`（或 contract 段存在）→ 解析 v2 → 生成 adapter_config 草案 → 返回给前端展示（含占位符校验结果）→ 人工确认后写入 `Agent.adapter_config` + sync 接口。
- **决策点**（已确认，按推荐项落地）：
  1. 落库端点 body 收**完整 v2 manifest**（真相源），adapter_config 由服务端 `build_adapter_config` 权威生成（Q1 硬错误闸门兜底）✅
  2. **可编辑后确认**：前端编辑 manifest contract 段后回传，服务端重新派生+校验 ✅
  3. manifest 快照内嵌 `adapter_config._manifest_v2`（ConfigEngine 忽略未知键，零迁移）✅
  4. discover adapter 硬错误**软语义**：`ok:true + adapter.valid=false`，接口/场景照常发现 ✅
  5. discover 不并入 probe（保持独立，wizard 编排）✅
- **已落地**：`ae1cffc`——`discover` v2 分支（`_is_v2_manifest`/`_adapter_block`，防御非 dict payload 不 500）+ `POST /agents/{id}/adapter`（`_confirm_adapter_check`）；单测 9 + 集成 6（discover v2 / adapter 落库 / 非 dict 不 500）；全量单测 511 / 集成 80。
- **状态**：✅ 完成

## Q3 4 家迁移 v2 + seed 适配（单一真相源）

- **现状**：4 家 `contracts.py` 是 v1；`seed_data.py` 手写 CS/CC/GQ/SP_ADAPTER_CFG；两处并行维护（注释「interfaces[].path 与平台 seed_data 一致」靠人工）。
- **方案要点**：4 家 contracts.py 升 v2（补 contract 段：prepare/request/sse/field_map/timeout + probe.input）；seed_data 的 adapter_config 改为**由 manifest 派生**（消除手写）；contract_version 升 2.0。
- **核心决策：adapter_config 静态化机制**（关键）：
  - seed 时 agent 未必在线，不能实时拉 manifest → 需一份静态快照
  - 方案 A：platform 侧内置 4 家 v2 manifest 快照（含 contract 段），seed/discover 从快照 build adapter_config，discover 时与 agent 运行时 /api/contracts diff 提示漂移（单一真相源 = manifest，快照 + 运行时双校验）
  - 方案 B：保持 seed_data 手写，仅 agent 侧升 v2 —— 未消除双维护，不符合「统一标准」目标
  - 推荐 A。待确认 4 家 manifest 快照放哪（seed_data.py？独立 constants？）
- **决策点**：4 家迁移后 seed 行为回归验证口径（集成测试 + 真实 run？）；contract_version 统一 "2.0"
- **状态**：⬜ 待方案确认 → 待实现

## Q4 文件型接口探测输入（cc 类）

- **现状**：`probe_interface` 对 cc 需 `{case.input.file_path}` 上传真实文件；接入时无上传文件，冒烟死角。
- **方案要点**：探测样例文件机制——平台 uploads 目录预置样例文件（或 adapter_config.probe.input 支持文件路径 + 平台校验存在性）；探测输入构造失败与契约不达标在响应中区分。
- **决策点**：样例文件来源（seed 时上传 / 平台内置 / agent 自备路径）；文件型接口冒烟的最小成本（cc 每次冒烟都要真上传+轮询）
- **状态**：⬜ 待方案确认 → 待实现

## Q5 用例骨架半自动

- **现状**：suite/case 全手工或 seed 示例。
- **方案要点**：v2 的 `{{input.*}}` 域推导 case 输入模板 + scenes 生成示例 suite/case 骨架（input 有值、expected/golden_answer/reference_docs 留空待填）。
- **上限声明**：`expected/golden_answer/reference_docs` 是业务知识，**永远人工**；自动化的终点是「用例骨架 + 冒烟通过」。
- **决策点**：骨架生成放后端（接口）还是前端（模板拼装）？与 wizard（Q7）的集成方式
- **状态**：⬜ 待方案确认 → 待实现

## Q6 新 agent 接入文档

- **现状**：README 接入说明 + good-question `verify_contract.py`（agent 侧自测样例）。
- **方案要点**：新 agent 接入指南——实现 v2 manifest 端点（含 contract 段）+ SSE 事件流符合 §5.1（meta/usage/done 必选、content+delta 双字段、tool_call 字段、事件带 ts）；以 4 家 v2 为样例。
- **决策点**：文档放项目 README 还是独立 docs？是否附带 agent 侧模板仓库/脚手架
- **状态**：⬜ 待方案确认 → 待实现

## Q7 前端接入 wizard

- **现状**：Agent 管理页，discover/probe/sync/scenes/auth 均为独立 API，无一体化流程。
- **方案要点**：前端接入流程：填 URL → 自动 discover（拉清单 + diff）→ 确认接口/场景 → 配置凭证 → 冒烟探测 → 生成 adapter/用例骨架 → 落库。把现有 API 串成向导。
- **依赖**：Q1/Q2/Q3/Q5。
- **状态**：⬜ 待 Q1-Q5 就绪 → 待方案

## Q8 测试闭环

- 各 Q 落地时配套：v2 解析单测（Q1 ✅ 25）+ scaffold v2 集成（Q2 ✅ 集成 6）、4 家迁移回归（Q3）、wizard 前端测试（Q7）。
- 测试口径：单测 `pytest -p no:html tests/`；集成 `RUN_INTEGRATION=1 DB_HOST=127.0.0.1 DB_PORT=33061 DB_USER=evaluation DB_NAME=ai_evaluation DB_PASSWORD=$(grep '^MYSQL_EVAL_PASSWORD=' ../../infra/.env | cut -d= -f2) python -m pytest -p no:html tests/integration/`（需先 `docker start shared-mysql`）。
