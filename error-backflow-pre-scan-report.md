# error-backflow pre-scan 报告（R-20 / G0 门禁）

> **本文件是什么**：`error-backflow-solution_detail.md` §9.4 要求的 **pre-scan report**，
> 门禁身份 = **G0**（`error-backflow-task.md:15`）。用途 = **在 `R-12` 改动 `text.py` 之前**，
> 证明「收紧空答语义」不会误伤存量。**未过 G0 不得改 `text.py`**（详设 §9.4 / `:21`）。
>
> **产出时间 / 基线**：**2026-09-15**。库 = `ai_evaluation`（offline 平台库，容器 `ai-eval-backend`）；
> 代码面未动（本报告**不改 backend 代码**，符合 `task.md:29` 的「代码落点」约束）。
> ⚠️ **本报告是一次性交付物，结论只对上述基线的存量成立**——见 §4 边界声明。

---

## 1. 为什么需要 G0（重申风险，不重复设计）

`R-12` 把 `KeywordNotContainsOp` 的空答语义从「空答 = 不命中 = **PASS**」翻转为「空答 = **FAIL**」。
该算子是 **manual / held_out 与 error_regression 共享**的（`text.py`），所以翻转会**同时**作用于存量普通 case：

- 对 error_regression 是**目标行为**（复现空答 = leakage 复发证据 → fail；详设 §9.2）；
- 对存量普通 case **可能是误伤**（某些 suite 的合法业务语义本就允许空答）。

⇒ 必须**先实扫存量、登记受影响面**，再决定「直接收紧」还是「豁免清单先行、分批合入」（详设 §13 行 12）。

---

## 2. 取证方法与基线（数目全部由命令产出）

**取证方式**：在 `ai-eval-backend` 容器内经 SQLAlchemy 直读 MySQL，**只读查询、无写入、无 DDL**；
凭据取自容器内 `os.environ`，未回显。全部数字可由下节各问内联的命令复算。

| 事实 | 值 |
|---|---|
| `test_case` / `test_suite` / `eval_run` / `eval_result` 行数 | 136 / 76 / 292 / 804 |
| `test_case` 中**含 keyword 断言**的 case | **85** |
| 其中**曾被跑过**（`eval_result` 有对应行） | **81** |
| **从未跑过** | **4**（case id = `3635`、`3636`、`3754`、`3755`） |
| 含 keyword 断言的 **suite** 数 | **17** |
| `eval_result.assertion_results` 里的 keyword 断言**结果**条数 | **780**（`keyword_contains` 672 + `keyword_not_contains` 108） |

> **⚠️ 一处 doc↔实现不符（新发现，2026-09-15）**：详设 §9.4 第 1 问写「扫现有 **`assertion_results`** / case.assertions」，
> 措辞像是**一张表**——`ai_evaluation` 库中**没有 `assertion_results` 表**（表清单见上）。
> 实际它是 **`eval_result` 的一个 JSON 列**，结构 = `{op, args, pass, actual, expected, dimension, source_detail}`。
> 照字面找表会卡住。**本报告按列实现，不改文档**（该订正建议另立条目）。

---

## 3. 三问三答

### Q① 存量依赖盘点 —— **受影响面 = 0（已观测面内）**

**问题**：现网**空字段回答**被 keyword 断言放行（PASS）的用例有多少、依赖面在哪（哪些 suite/agent/case）。

**答**：**0**。全量 780 条 keyword 断言结果交叉表如下（`pass` × 值是否空白）：

| op | pass=True | pass=False |
|---|---|---|
| `keyword_contains` | 660（全非空） | 12（全非空） |
| `keyword_not_contains` | 92（全非空） | 16（全非空） |
| **空白的** | **0** | **0** |

**关键区分（决定了这条证据的强度）**：不是「有空白但都被放行了」，而是**空白一次都没出现过**。
该字段在 780 条结果里 **100% 存在**（`keys_seen` 实测 = `{op:780, args:780, pass:780, actual:780, expected:780, dimension:780, source_detail:780}`）
⇒ **排除了「取值路径取不到 ⇒ 恒空 ⇒ 假零」这一可能**，结论不是取证失误。

**副产物（顺带否掉一个反面假设）**：28 条 keyword 断言**是 fail 的**（12 + 16）⇒ 断言**有判别力**，
不是恒 PASS 的摆设；「空答 0」不是「断言从不判负」造成的。

> ⚠️ **上段已订正（2026-09-15，R-12 落地批）—— 基数与结论都变了，原文留存勿引**：
> 逐条回查那 28 条的 `actual` 后发现，**16 条根本不是「判定失败」**，而是
> `actual='<断言算子未注册: keyword_not_contains>'` —— **O-C.5 登记该算子之前的历史行**，
> `pass=False` 来自「算子不存在」而非「判定命中」。**真正的判定失败只有 12 条，且全部是
> `keyword_contains` 的**。
>
> **⇒ 订正后的严格表述**（结论**部分反转**）：
> - 「断言有判别力」这条论据的**有效基数 = 12（全部属于 `keyword_contains`）**，不是 28；
> - **对 `keyword_not_contains` 本身，本报告提供不了任何判别力证据** —— 存量里它跑过
>   **92 次、真判负 0 次**（另 16 条不构成判定）。即：**「空答 0」这条结论所依赖的那个算子，
>   其「能不能判负」在存量中是未证状态**，本报告只能说「未观察到空答」，
>   **不能说**「该算子被证明会判负」。
> - **这不削弱 Q① 的结论**（受影响面 = 0 仍成立：它是「没出现过」的观测，不依赖算子会判负），
>   但**原先那句「「空答 0」不是「断言从不判负」造成的」在 `keyword_not_contains` 上恰好落空** ——
>   它在存量中确实**从未判负过**，无法用它排除「恒 PASS 摆设」这一假设。
> - **判别力因此必须来自别处**：R-12 落地时以**注入式反事实对照**取得（摘掉守卫 ⇒ 新增用例全红，
>   红在「期望 False 实得 True」），见 `error-backflow-task.md` O-E.9 施行记录。**这正是本报告
>   §4 第 3 条自陈「本次未做注入式对照」所欠的那一块，已由落地批补上。**

### Q② 合法空答白名单 —— **空集（依据见下，非主观认定）**

**问题**：哪些 manual/held_out 存量 suite/case（**非 error case**）的合法业务语义**允许空答**。

**答**：**白名单 = 空集**。依据**不是**「我认为不需要」，而是**受影响面为空**：

详设 §9.4 Q② 自己限定了评估对象 = 「**受影响 suite**」（原文：「存量 keyword 断言 suite
**现空答 PASS、收紧后 FAIL，才是需白名单的受影响面**」），并要求「**pre-scan 逐受影响 suite 评估**后登记清单」。
⇒ 受影响面的**定义**依赖 Q① 的观测结果；Q① = 0 ⇒ **受影响 suite 集合为空** ⇒
**Q② 的被评估者集为空，白名单无对象可列**。这是**按规格执行得到空集**，不是用数据结论替代语义判断。

**⇒ 落位形态**：按详设 §13 行 12 的裁决口径，本情形属「**受影响面小 → 直接收紧**」一档，
**不启用豁免清单机制**（也就不需要在断言逻辑里加豁免载体）。

### Q③ 上线形态 —— **确认按详设原文，无偏离**

**答**：非白名单 case 空答从「`keyword_contains` 天然 PASS / `keyword_not_contains` 误 PASS」
收紧为**显式判定**；白名单 case 空答走豁免。**本报告不改变该形态**；
因 Q② 为空集，**本期无「白名单 case 空答走豁免」的实际作用对象**（该分支保留为机制、当前无输入）。

---

## 4. 边界声明（**引用本报告前必读**）

1. **只对 2026-09-15 的存量成立**。上述「零空答」是**对存量数据的观测**，
   **不等于**「空答不会发生」。`R-12` 的价值在**未来**防漏判——**本报告不得被读作「R-12 可以不做」的依据**。
2. **有 4 个 case 未被观测**（`3635` / `3636` / `3754` / `3755`：含 keyword 断言但 `eval_result` 中无行）。
   ⇒ 严格表述是「**已观测的 81/85 面上，空答 = 0**」，而非「全部存量 = 0」。
   本报告**不假设**这 4 个的未知态，也**不**据此宣称已覆盖。
   处置见 §5 的 G0 裁决建议（二选一，**须显式选，不得默认**）。
3. **判据的判别力来源 = 存量数据本身**，本次未做注入式对照（不适用：本报告是**盘点**不是**机制验证**，
   无「应命中却未命中」的待验证路径）。
4. **未核**：是否存在**不经 `eval_result` 落库**的判定路径（如早期 run 或异常终止 run 未写 `eval_result`）。
   本报告只覆盖**已落库结果**；若存在该路径，其空答行为不在本报告观测面内。

---

## 5. G0 裁决

**门禁条件 = 「报告三问有结论 + 白名单豁免落位」**（详设 §9.4）：
- 三问有结论 → ✅（Q①=0、Q②=空集、Q③=照原文）
- 白名单豁免落位 → ✅（**空集落位**：按 §13 行 12 归「受影响面小 → 直接收紧」，不启动豁免机制）

⇒ **G0 建议判「过」**，`R-12`（`O-E.9`）解除门禁。

**⚠️ 但「过」有一个前提须由用户显式选择**（§4 第 2 条，不得默认）：

- **选项 A**：接受「81/85 已观测面零空答」为充分证据 → **G0 直接放行**，
  4 个未跑 case 的空答行为**留作 R-12 上线后的自然观测**（若它们将来出现空答，按新语义判 FAIL，属**预期行为**）。
- **选项 B**：先补跑那 4 个 case 再放行 → 得到 85/85 全覆盖，代价 = **一次 4 case 的重跑**。

两者对 `R-12` 的正确性**无实质影响**（未跑 case 将来按新语义判 FAIL 正是 R-12 的**目标行为**），
差别只在**证据完整度**。**推荐 A**（B 的收益是形式覆盖，不改变结论）。

### 5.1 裁决结果（2026-09-15，用户拍板）

**裁决 = 选项 A**。G0 判「过」，`R-12` 解锁。裁决时点另查得三条事实，**部分推翻了本节上文的表述**，
一并订正（原文保留于上，勿回改）：

1. **4 个未跑 case 中有 2 个根本不属于本门禁的问题域**：`3635`（suite 2603）与 `3636`（suite 2604）
   是 **error suite 的 case**（`test_suite.is_error_suite=1`、`case_type='regression_error'`）。
   R-12 对它们**本就以 FAIL 为目标**，不进「误伤面」——本节上文 §4 第 2 条与 §5 把它们并称
   「4 个未跑 case」，掩盖了这一区分。
2. **真正未观测的普通 case 只有 2 个**：`3754`/`3755`（同属 suite 2733，agent 2941，`case_type='factuality'`），
   而该 suite **一次都没跑过**（agent 2941 下 `eval_run` 计 0）。⇒ 其空答行为**无法从存量取得证据**；
   补跑是「造新数据」而非「取存量证据」，与本节把 B 写成「再取一次存量证据」的定性不符。
3. **上文把 B 的代价写成「一次 4 case 的重跑」属低估**：suite 2603/2604 已有各 3 次 run，
   末态分别为 `timeout` / 卡 `running`，且**结果行均为 0** ⇒ 那部分不是「重跑一次」而是
   「先查清为什么跑不出结果」。加上第 1 条，这部分的收益与门禁问题域无关。

**⇒ 严格表述应为「81/83 问题域相关面上空答 = 0」**（83 = 85 − 2 个 error suite case），
而非本节上文写的「81/85」。裁决取 A 不受此影响：剩余 2 个普通 case 属从未运行过的 suite，
二者都取不到证据，且 B′ 还须先回答「agent 2941 为何从未运行」。

---

## 6. 交付与关联

- **本报告 = G0 出口物**，产出后**未改任何 backend 代码**（详设 §9.4 对代码落点的约束）。
- **关联**：`error-backflow-task.md` O-A.1（本条）· `error-backflow-solution_detail.md` §9.4 · §13 行 12 ·
  `R-12` = 横切表 `R-12` 行（`text.py 空答 fail 修补`）。
- **本报告的自我限制**：§2 末尾那条 doc↔实现不符（`assertion_results` 是列不是表）**只登记、未改文档**，
  是否回填详设由用户裁。
