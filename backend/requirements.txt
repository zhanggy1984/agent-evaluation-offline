# 顶层依赖声明（pip-compile 源文件）：Docker 构建用 requirements.lock（pip-compile 生成，
# 精确钉死版本保证可复现构建）。改这里后重新执行：
#   pip install pip-tools && pip-compile requirements.txt -o requirements.lock
# P2-D13：python-multipart 下界 >=0.0.18（CVE-2024-53981 DoS 修复版本，堵旧版漏洞）
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
sqlalchemy[asyncio]>=2.0,<3
aiomysql>=0.2,<1
alembic>=1.13,<2
pydantic>=2.7,<3
pydantic-settings>=2.2,<3
httpx>=0.27,<1
cryptography>=42,<44
PyJWT>=2.8,<3
bcrypt>=4.1,<5
python-multipart>=0.0.18,<1
apscheduler>=3.10,<4
greenlet>=3.0,<4
reportlab>=4.2,<5
