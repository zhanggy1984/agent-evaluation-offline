# 硬伤优化技术方案：#3 定向重跑 + #6 N/A 保护 + #2 门禁区间化

> 依据：`评测平台分析-硬伤与优化清单.md`（#1 多数决已完成；本批为「只做 3 件事」剩余组合）。
> 状态：**已定稿**。独立 Plan subagent 评审（5 P1 + 10 P2）全部回应；4 个决策点用户已拍板（2026-08-31）：A1/B1/C1/D1。
> 三块相互独立、可分批交付：#3（run 执行层）、#6（scorer gate 前置）、#2（scorer gate 判定）。

---

## #3 run 定向/子集重跑

### 现状（file:line）

- `api/runs.py:46-51` `RunCreate` 仅 `agent_id/suite_id/version/trigger_type(manual|held_out)`，**无 case 子集参数**
- `api/runs.py:180-206` `rerun_run` 复制 suite/version/config **全量重跑**，无定向
- **case 加载过滤共 4 处**（评审 P1-1 纠正，方案原只列 2 处）：
  - `runner/orchestrator.py:137-139` 执行加载
  - `runner/orchestrator.py:276-278` `_probe_before_run` 跑前探测加载
  - `runner/orchestrator.py:497-499` `_finish` 对账回填
  - `runner/scorer.py:338-340` `score_run_salvage` 超时兜底回填
- 互斥 `runs.py:110-114`：同 agent 单 in_progress run（`_ACTIVE_STATUS=(pending,running,scoring)`）
- **已有可复用机制**：`trigger_type="held_out"` 已实现「按标志过滤 case 子集」——#3 是其泛化（按 id 列表过滤）

### 问题

一次全量 run 2-4h（`orchestrator.py:218-221` 受 `per_agent_concurrency=3` 限制），修一个 case 后只能全量重跑，迭代成本极高。

### 设计

**A. `RunCreate` 加可选 `case_ids: list[int] | None = None`**（`runs.py:46`）
- `None`=全量（现状不变）；非空=只跑这些 case
- 校验（创建接口内，`runs.py:98-103` 现有校验旁）：
  - **`set()` 归一去重**（评审 P2-9）
  - 每个 id 必须属于 `body.suite_id` 且 `status=="active"`
  - `is_held_out` 必须匹配 `trigger_type`（held_out run 只接受 held_out case，反之亦然）
  - 交集为空 → `400 E_VALIDATION`（避免静默跑 0 case 的 completed）

**B. 子集事实落库：EvalRun 加 `case_ids` JSON 列（可空）**（`models/run.py:20-59`，需迁移）
- **不进 `run_config`**：`run_config` 是 scope=run system_config 冻结快照（`runs.py:54-57`），塞 case_ids 污染配置语义。评审 P2-10 提出替代路线（run_config 键免迁移，rerun 复制自动继承），**取舍见「决策点 D」**
- `run_out`（`runs.py:67-87`）返回 `case_ids`；**`redact=True`（留出集 owner 隐藏）时置 None**（评审 P2-8：防泄露留出集内容指纹），前端子集标识在裁剪态显示为空

**C. 四处 case 加载统一为单一函数 `_load_run_cases(db, run)`**（评审 P1-1 必须）
- 新模块 `app/runner/case_loader.py`（orchestrator/scorer 共同 import，**无循环依赖**：scorer 不反向 import orchestrator）
- 内部实现：`select(TestCase).where(suite_id, status=="active", is_held_out==(trigger_type=="held_out"))` + `case_ids 非空 → id.in_(case_ids)`
- 替换 `orchestrator.py:137-139`、`:276-278`（probe 用子集 case 探测，非子集接口损坏不再卡死子集 run）、`:497-499`、`scorer.py:338-340`
- **TOCTOU**：case 在创建后被置非 active → 过滤静默剔除 → `total_case` 变小但 `case_ids` 列保留原始请求，`_finish` 对账不再补它（评审 P2-9，可接受降级，文档化）

**D. `POST /runs/{run_id}/rerun` body 加可选 `case_ids`**（`runs.py:180-206`）
- **`None` = 继承 `src.case_ids`**（评审 P2-9 修正原「None=全量」）：普通 run 重跑=全量（src.case_ids 为 None），子集 run 重跑=同一子集——「重跑同一批」直觉一致，前端子集 run 点重跑不会意外扩成全量
- 非空=定向改批（前端从 `run_results` 勾选 fail/na case → 传 ids 复测；单 case 重跑即 `case_ids=[id]`）
- `src.case_ids` 为 None 时 `run_config` 复用冻结配置（`runs.py:196` 现状）

**E. 互斥与 #4 的关系（不并入本批）**
- 子集 run 仍占 agent 执行槽（`runs.py:110-114`）——定向重跑价值最大化依赖 #4「scoring 不占执行槽」，独立优化本批不动

### 落库路线（已定：D1 EvalRun 新列）

EvalRun 加 `case_ids` JSON 可空列 + alembic 迁移。否决 run_config 键路线（污染快照语义——orchestrator 已往里写 fail_reason 等 `orchestrator.py:300-303`，且 `_run_out` 需读 run_config 增耦合）。

---

## #6 case 级 N/A 保护

### 现状（file:line）

- `runner/scorer.py:92-95` N/A 维度剔除后**重归一化**加权；`scorer.py:105-111` 门禁**跳过 N/A 维度**
- **run 级**有 `judge_incomplete` 兜底（`scorer.py:304`，分母守卫 `total_acc_w > 0`；`runs.py:84`），**case 级无**——case 的 factuality（权重 0.35，`constants.py:88`）judge 全失败时该维度不参与评分也不参与门禁，case 仍可判 pass（P1-4 硬伤）

### 关键事实（评审确认：区分「该判没判」vs「刻意不启用」）

`semantic_na_weight` 只累计**启用且 judge 失败**的语义维度（`scorer.py:92-95` + `_enabled_dims:135-140`）：
- case 的 `metrics` 禁用 factuality → 不进 `_enabled_dims` → 不计 na → 不触发保护（✓ 刻意不启用不受影响）
- case 启用 factuality 但 judge 全失败 → 计 na → 触发保护（✓ 本应判分没判出来）

### 设计

**A. 复用已有 `judge_na_threshold` 配置（评审 P2-3，不新增）**
- `seed.py:365-366` 已注册 `judge_na_threshold`（scope=run, 默认 0.3, is_hot），`scorer.py:253` 已从 run_config 读取做 run 级判定
- case 级与 run 级**同阈值同配置**（语义一致：缺失语义分量权重占比阈值）；免新配置 + seed + 存量 SQL + `sysconfig_schema.py:13-47` meta 契约四处改动

**B. `scorer._score_case` 门禁后置保护（评审 P1-4 分支顺序，必须门禁后）**

判定顺序（`scorer.py:80-124`）：
```
score_total is None                     → na（metric_na，现状）
gate_failed                             → fail（现状）
total_accuracy_weight > 0 and
  semantic_na_weight / total_accuracy_weight > judge_na_threshold
                                        → na（na_reason="semantic_na_high"）
else                                    → pass
```
- **必须放门禁后**：否则门禁失败的 case（如 completeness 断言 fail + 语义 N/A）会被保护掩蔽成 na，`fail_case` 下降、评测侧按 fail 筛选修复的流程漏掉它。放门禁后只有「本会 pass」的 case 变 na
- **除零守卫 `total_accuracy_weight > 0`**（评审 P1-5）：`set_agent_weights`（`agents.py:352-366`）/`set_interface_weights`（`:377-390`）权重无下限校验，四维全 0 是合法输入 → 0/0 崩溃；对齐 run 级 `scorer.py:304` 守卫
- **分母口径 = `total_accuracy_weight`（全四维权重和，`scorer.py:99`）**（评审 P2-2 回应）：与 run 级 `judge_incomplete` 同口径，避免 run/case 两级口径分裂；「缺失占比」相对满配四维（非该 case 启用域）的失真接受并文档化

**C. 阈值不对称（评审 P2-1；已定 A1）**
- 占比阈值 0.3 下，reasoning 单独失败（权重 0.15 < 0.3）**不触发**，静默 pass；factuality 单独失败（0.35）触发——不对称
- **已确认（决策点 A1）**：保持占比阈值，不对称作为已知行为文档化——只有大权重维度（factuality）判分失败才标 na，误伤面最小

### 行为变更（必须评审）

- judge 未判的 case：从「静默 pass」→「na 待复核」；**放门禁后不掩蔽门禁 fail**（评审 P1-4 修正）
- **破坏的测试**（评审 P2-7 枚举，不止方案原列一个）：
  - `test_scorer.py:30-42` `test_full_pass_rules_only`（断言全过 + judge_results=None → ratio 0.5 → pass→na）
  - `test_scorer.py:93-99` `test_na_dimension_not_gated`（同场景 → pass→na；该测试本身是「N/A 不参与门禁」旧哲学，与 #6 直接冲突，须适配）
- 上线后 run 的 pass 数可能下降（被 na 取代）——评测侧需感知

---

## #2 门禁区间化（档位化判定）

### 现状（file:line）

- `judge/rubric.py:19` `RATINGS=(0.0,0.2,...,1.0)` → judge 输出分数集合 `{0,20,...,100}`（离散 6 档）
- `runner/scorer.py:109` 门禁 `entry["value"] < target`；target 是**连续分**（`agents.py:407-438` 双签标定，可任意如 85/92）
- **悬崖**：target=85 时只有 100 能过（80<85），「基本准确」(80) 与「完全准确」(100) 一档之差决定门禁生死

### 关键设计约束（评审确认）

**档位化只对语义维度（factuality/reasoning_quality）生效**：
- 语义维度 value 恒为 20 倍数：`client.py:200` `score=RATINGS[level]*100`、`aggregate.py:44` 同口径、`factuality.py:17-22`/`reasoning.py:16-21` 直读无插值 ✓
- **规则维度（completeness/tool_usage）value 是连续通过率**（`completeness.py:25`/`tool_usage.py:20` `passed/total*100`）——归档会反向变严（79% 通过率被归档到 3 档 fail），必须保持连续分比较 ✓

### 设计

**A. 抽单一判定函数 `_gate_met(value, target, dim) -> bool`**（评审 P1-2 四处同源）
```
语义维度：int(value // 20) >= int(target // 20)     # 档位化：target 归 floor 档
规则维度：value >= target                            # 连续分：现状不变
边界兜底（评审 P2-5）：value/target 为 None → 视为不达标（fail 方向保守）；
负数 → 档位化语义未定义，按现状连续分比较兜底
```

**B. 四处同源替换**（消除口径分裂）：
1. `runner/scorer.py:109` 门禁主判定（`gate_failed = not _gate_met(value, target, dim)`）
2. `api/runs.py:283` `run_failures` 的 `fail_dims`（判定 value<target 列失败维度，同步 `_gate_met`）
3. `app/core/dashboard_rules.py:161` `build_baseline.met`（`gap, met = round(score-target,2), score>=target` —— **必须同步**，否则 run 详情 pass 但基线页 met=False 口径分裂）
4. `resolve_targets`（`dashboard_rules.py:89-95`）纯解析无判定，**不用改**（评审确认）

**C. `set_targets` 补 target_score 0-100 范围校验**（评审 P2-5：现无范围校验，负数/超界合法入参；`agents.py:407-438`）

**D. 不改变 target 存储语义**：双签值不变，仅判定层档位化——85 展示仍是 85，门禁按「达到 4 档（≥80）」判

### 等价性矩阵（语义维度）

| target | 档位 floor(target/20) | value 80 | value 100 | 与现状差异 |
|--------|---------------------|----------|-----------|-----------|
| 60 | 3 | pass | pass | 一致 |
| 80 | 4 | pass | pass | 一致（边界） |
| 85 | 4 | **pass**（现状 fail） | pass | **悬崖消除** |
| 92 | 4 | **pass**（现状 fail） | pass | 高 target 放宽到 80 档（⚠️ 见「决策点 B」） |
| 100 | 5 | fail | pass | 一致（满分仍严格） |

### 与 `scripts/_gate_majority.py` 的关系（评审 P2-4 纠正）

- **该文件在本仓库 `scripts/_gate_majority.py`**（非「评测侧外部机制」，评审查证 test_gate_majority.py 同源）
- 它按 `>= target` 判，judge 维度 80 对 target 85 仍不过——平台内判 pass（80）但发布规程仍卡 85，悬崖残留
- **已定（决策点 C1）：本批同步档位化**——`scripts/_gate_majority.py` 的 `>= target` 改 `_gate_met` 同源逻辑，平台与发布规程口径一致，悬崖全链路消除

---

## 改动文件清单（评审 P1-3 修正：前端是本仓库工作）

| 文件 | 改动 |
|---|---|
| `app/runner/case_loader.py` **新增** | `_load_run_cases(db, run)` 统一四处 case 加载（#3 C） |
| `app/runner/orchestrator.py` | 三处加载替换为 `_load_run_cases`（L137/L276/L497） |
| `app/runner/scorer.py` | #3 salvage 加载替换 + #6 门禁后置保护（含除零守卫）+ #2 门禁判定 `_gate_met` |
| `app/api/runs.py` | RunCreate.case_ids、rerun body.case_ids（None=继承 src）、校验、run_out.case_ids（redact 置 None）、run_failures 同步 `_gate_met` |
| `app/core/dashboard_rules.py` | `build_baseline.met` 同步 `_gate_met` |
| `app/models/run.py` | EvalRun 加 `case_ids` JSON 列（已定 D1） |
| `migrations/` | 新增：`ALTER TABLE eval_run ADD COLUMN case_ids JSON NULL`（已定 D1） |
| `app/api/agents.py` | `set_targets` target_score 0-100 校验（#2 C） |
| `scripts/_gate_majority.py` | 档位化同步（已定 C1） |
| `frontend/src/views/Dashboard.vue` | createRun 传 case_ids（勾选）、rerunRun 传 `{case_ids}`；子集 run 标识 |
| `frontend/src/api/runs.js` | 两个 API 函数签名扩展 |
| `tests/test_scorer.py` | #6 适配 test_full_pass_rules_only + test_na_dimension_not_gated；#2 判定矩阵用例 + `test_gate_uses_aggregated_majority_score` 断言翻转（fail→pass，**正是 #2 验证点**） |
| `tests/test_runs.py` | #3 case_ids 校验/子集流程/rerun 继承 |
| `tests/test_run_timeout.py` | 子集 run 超时估算（semantic_tasks 按子集，`orchestrator.py:162` 已天然按过滤后 cases） |

## 测试

- #3：子集 run 只执行指定 case；**probe 只探测子集 case**（非子集接口损坏不卡死）；rerun 继承子集/定向改批；空子集 400；去重/held_out 校验拒绝；salvage 不回填子集外 case；run_out redact 隐藏 case_ids
- #6：ratio 超阈值 → na（factuality 单独失败 35% 触发）；禁用维度不触发；**门禁 fail 不被掩蔽**（completeness 断言 fail + 语义 N/A → 仍 fail）；`total_accuracy_weight=0` 不崩
- #2：判定矩阵（60/80/85/92/100 × 80/100）；规则维度连续分不受影响；build_baseline.met 与门禁同源；run_failures fail_dims 同源
- 回归：#1 全部用例 + 全量单测/集成

## 风险

- **#6 是行为变更**：judge 未判的 case 从 pass→na；pass 数下降需评测侧感知（门禁 fail 不被掩蔽，已控制破坏面）
- **#3 迁移**：新增可空 JSON 列低风险（`models/run.py` 已有 2 JSON 列先例）；`alembic/versions/` 已有 8 个 revision 迁移链
- **#2 高 target（92）放宽到 80 档**：已确认统一 floor 归档（决策点 B1）——92 目标也只要 80 档，语义「达到 floor 档即 pass」
- **#2 + `_gate_majority`**：已确认本批同步档位化（决策点 C1），平台与发布规程口径一致，悬崖全链路消除

## 验证

1. 单测全绿（含三块新增 + #1 回归）
2. 集成：子集 run 真库执行、judge 全失败 case → na、门禁 fail 不掩蔽、档位化端到端 + build_baseline 同源
3. 迁移：`alembic upgrade head` 后 `eval_run.case_ids` 可空兼容旧行
4. 存量库无数据迁移（case_ids 新列可空；judge_na_threshold 已有配置无需 SQL）

---

## 决策点（已定稿，2026-08-31 用户拍板）

- **A1（#6 阈值不对称）**：保持 `judge_na_threshold` 占比 0.3，不对称明示文档化——factuality（0.35）单独失败才触发；reasoning（0.15）单独失败不触发（已知行为）
- **B1（#2 高 target 语义）**：统一 floor 归档——92 目标与 85 同为 4 档，value 80 即 pass；语义「达到 floor 档即 pass」
- **C1（#2 同步 `_gate_majority.py`）**：本批同步档位化，平台与发布规程口径一致，悬崖全链路消除
- **D1（#3 落库路线）**：EvalRun 加 `case_ids` JSON 可空列 + alembic 迁移，否决 run_config 键路线

---

*方案定稿 v3：2026-08-31。独立评审 5 P1 + 10 P2 全部回应；决策点 A1/B1/C1/D1 用户已确认，按此实施。*
